# HealConnect Automation Testing

Simple Selenium + pytest project for testing HealConnect.


## Setup

```bash
pip install -r requirements.txt
```

## Run tests

Make sure the HealConnect app is running at:

```text
http://127.0.0.1:8000
```

Then run:

```bash
pytest
```
